{{define "example.cmd.go" }}
package main

import (
	"{{.RepoName}}/gen"
)

func init() {

    // define service
	service := gen.NewEmptyCmd(&Gen,
        "cmd",                        // service name
        "github.com/nerv/servicecmd", // repo name
    )

    // add command
    service.Set("banner", "figlet $input")

    // register service
	Gen.AddConfig(service)

}
{{end}}
